<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
  <%- include('templates/head.ejs') %>
</head>

<body>
  <%- include('templates/header.ejs', {active: 'home' }) %>
  <main>
    
    <% if(houses.length) { %>
    <div class="flex-container">
      <% for(let house of houses){ %>
      <div class="card">
        <img src="<%= house.houseImageUrl %>" alt="<%= house.title %>" style="width:100%">
        <h1><%= house.title %></h1>
        <p> <i class="fa fa-fw fa-usd"> </i>Price:£ <%= house.price %></p>
        <p><i class="fa fa-fw fa-bed"> </i> Number of Beds: <%= house.beds %></p>
        <p> <i class="fa fa-fw fa-bath"></i> Number of Baths: <%= house.baths %></p>
        <p> <i class="fa fa-fw fa-edit"></i> Number of Receptions: <%= house.receptions %></p>
        <p> <i class="fa fa-fw fa-map"></i> Address: <%= house.address %></p>
        <p> Description: <%= house.description %></p>
        <p><a class="button" title="Delete" href="/delete/<%= house._id %>" onclick="return confirm('Are you sure you want to delete this item')"><i class="fa fa-fw fa-trash"></i></a> 
          <a class="button" title="Edit" href="/add/<%= house._id %>"><i class="fa fa-fw fa-edit"></i></a></p>
      </div>

      <% } %>
    </div>
    <% } else { %>
    <div>No House Price Available...</div>
    <% } %>
  </main>
  <%- include('templates/footer.ejs') %>
</body>

</html>